"""RevTrace backend application entrypoint.

Phase 0 placeholder. Intentionally contains no imports and no application
code: backend dependencies (FastAPI et al.) are declared in pyproject.toml
but are NOT installed yet, so any import here would fail.

The FastAPI application is created in Phase 1 (backend foundation and
database).
"""
